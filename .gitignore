node_modules/
dist/
*.log
.DS_Store
.claude/*.lock

# Vendor reference docs (Korg copyright) — keep local, cite URLs in specs
docs/hardware/

# Calibration rig: all exploratory sessions and raw captures stay local.
# `calib evidence <session>` explicitly promotes only derived artifacts into
# calib/evidence/, which is trackable; accepted manifests live in calib/results/.
calib/tmp/
calib/backups/
calib/sessions/
tools/calib/native/.bin/
